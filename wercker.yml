box: nodesource/trusty
build:
  steps:
    - install-packages:
        packages: google-chrome

    - npm-install

    - script:
        name: update webdriver
        code: |
          $(npm bin)/webdriver-manager update
    - script:
        name: update webdriver
        code: |
          $(npm bin)/run protractor
